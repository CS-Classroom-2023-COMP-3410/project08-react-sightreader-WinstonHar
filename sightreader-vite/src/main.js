// Import necessary modules and styles
import './sightreader.js';
import '../public/css/sightreader.css'; // Adjust path if needed
import '../public/css/preface.css';
import '../public/css/bootstrap.min.css';
import '../public/css/flat-ui.min.css';

// Optional: Import any additional external libraries or modules if needed
// For example, if you have installed jquery via npm, you might import it like this:
// import $ from 'jquery';

// Ensure scripts execute after the page loads
document.addEventListener("DOMContentLoaded", () => {
    console.log("Vite-powered Sightreader is initialized.");
    
    // Any other initialization code can go here.
    // For instance, if you need to call an initialization function from sightreader.js:
    // initSightreader();
});
